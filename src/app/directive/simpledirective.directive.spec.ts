import { SimpledirectiveDirective } from './simpledirective.directive';

describe('SimpledirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new SimpledirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
